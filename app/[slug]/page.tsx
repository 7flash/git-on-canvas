/**
 * Dynamic slug route — renders the same canvas page as root.
 * URL: /starwar, /galaxy-canvas, etc.
 * The slug is read client-side from window.location.pathname.
 */
export { default } from '../page';
