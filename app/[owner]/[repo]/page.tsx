/**
 * Dynamic owner/repo route — renders the same canvas page as root.
 * URL: /7flash/gitmaps, /facebook/react, etc.
 * The owner and repo are read client-side from window.location.pathname.
 */
export { default } from '../../page';
